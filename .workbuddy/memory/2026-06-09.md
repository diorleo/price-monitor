# 2026-06-09 工作日志

## Render + GitHub 部署配置

### 背景
用户想把 Price Monitor 后端部署到云端，选择 GitHub + Render（免费）方案。

### 新增/修改文件
- `.gitignore` — 忽略 venv、数据库文件、缓存等
- `Procfile` — Render/Heroku 标准启动文件（gunicorn）
- `render.yaml` — Render 自动部署配置（含 Disk 持久化 SQLite）
- `DEPLOY.md` — 完整部署步骤文档
- `backend/requirements.txt` — 加入 gunicorn>=21.2.0
- `backend/app.py` — 修改两处：①顶部加 `import os`；②启动改为 `int(os.environ.get('PORT', 5000))`；③init_db/seed_defaults/reschedule 移到模块级（gunicorn 也能初始化）

### 用户信息
- GitHub: diorleo（linchunbao2011@gmail.com）
- 目标仓库：diorleo/price-monitor
- 部署平台：Render 免费版

### 待用户操作
1. 在 GitHub 创建 `price-monitor` 仓库
2. git init → git push 到 diorleo/price-monitor
3. Render 关联仓库，配置 Build/Start Command 和 Disk
