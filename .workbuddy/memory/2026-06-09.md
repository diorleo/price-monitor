# 2026-06-09 工作日志

## 云端部署：GitHub + Railway（最终方案）

### 经过
- 用户 GitHub: diorleo（linchunbao2011@gmail.com）
- 仓库：diorleo/price-monitor（已推送，main 分支）
- 先尝试 Render，但因需要绑信用卡放弃
- 切换至 Railway（免费、无需绑卡、GitHub OAuth 登录）
- 两次失败后修复：gunicorn 找不到 → 改用 `python -m gunicorn`
- 第三次部署成功：Railway 显示 Online，1 replica running

### 新增/修改文件
- `.gitignore` / `Procfile` / `DEPLOY.md`
- `requirements.txt`（根目录，供 Railway 自动安装依赖）
- `runtime.txt`（指定 Python 3.13）
- `railway.json`（Railway 配置）
- `backend/requirements.txt` — 加入 gunicorn
- `backend/app.py` — 支持 PORT 环境变量、try/except 包裹启动初始化

### Git 代理
- 用户使用微兔代理，端口 127.0.0.1:8001
- `git config --global http.proxy http://127.0.0.1:8001`

### 最终结果（2026-06-11 更新）
- Railway 放弃（Nixpacks 强制 gunicorn，无法覆盖启动命令）
- 切回 Render，部署成功：https://moza-price-monitor.onrender.com
- /api/dashboard 验证正常（72产品、7渠道）
- 前端 API_BASE 已更新：GitHub Pages 环境自动指向 Render 后端，本地 localhost 仍指向本地 Flask
- GitHub Pages 前端：https://diorleo.github.io/price-monitor/（需在 repo settings 开启）
